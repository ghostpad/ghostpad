export type MsgLoadPopup = {
    call_back: string;
    deleteable: boolean; 
    editable: boolean;
    popup_title: string;
    rename_return_emit_name: string;
    renameable: boolean;
    upload: boolean;
}